<%= turbo_frame_tag :container do %>

  <div>
    <%= t('.edit_tax')%>:
    <%= render 'shared/errors', object: @tax %>
    <div>
        <%= render 'form', tax: @tax %>
    </div>
  </div>

<% end %>
