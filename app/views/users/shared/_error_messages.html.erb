<% if resource.errors.any? %>
  <% resource.errors.full_messages.each do |message| %>
    <%= render  "shared/toast", title: "Error - #{resource.class.model_name.human.downcase}", message: message, primary: 'red', complement: 'white', hover: 'black' %>
  <% end %>
<% end %>
